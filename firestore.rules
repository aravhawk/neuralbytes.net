rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side reads and writes.
    // Only the Firebase Admin SDK (used by Cloud Functions) can access data.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
