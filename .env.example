# Neon PostgreSQL connection string (provided by Vercel's Neon integration)
# When you link Neon via the Vercel dashboard, this env var is set automatically.
# For local dev, copy this file to .env.local and fill in your Neon connection string.
DATABASE_URL=
